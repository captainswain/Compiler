$$
    int x,  y, z;
    int low, high, step, a, b, c;
    a = b + c;
    x = 1 * 1 + 1 / 5;

    get (x, y, z);
    while (low  <  high )  
    {  put (low);
        put (low);
        low =  low  +  step;
    } 
    whileend

$$
 
